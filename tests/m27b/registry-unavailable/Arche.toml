schema = 1

[package]
name = "example/registry-client"
version = "0.1.0"
edition = "2026"
arche = ">=0.0.0"

[lib]
path = "src/lib.arc"

[dependencies]
remote = { package = "example/remote", version = "^1.0.0" }
